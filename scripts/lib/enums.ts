export enum Ctype {
    HTML = "text/html",
    JS = "text/javascript",
    JSON = "application/json",
    CSS = "text/css",
    PNG = "image/png",
}

export enum APIActions {
    ERROR,
    CONFIRM,
    SUCCESS,
    FAILURE
}