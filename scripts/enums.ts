export enum Ctype {
    HTML = "text/html",
    JS = "text/javascript",
    JSON = "application/json",
    CSS = "text/css"
}

export enum APIActions {
    ERROR,
    CONFIRM,
    SUCCESS,
    FAILURE
}